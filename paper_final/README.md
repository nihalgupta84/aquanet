# Final evidence-aligned manuscript

Title: **When Shared Hyperparameters Reverse Model Rankings: A Controlled Study of Visual Water-Condition Classification**

This draft supersedes `paper_v1`, `paper_v2`, and `paper_v3`. Its central claim is the measured protocol-sensitivity result, not a claim that AquaNet is the best architecture or that D1 is a newly introduced dataset.

Build from the repository root with `python experiments/generate_final_paper_figures.py`, then run pdflatex/bibtex/pdflatex/pdflatex in this directory.

Evidence sources are `reports/final/final_report.json`, `reports/stageD_*.json`, `reports/stageE_*.json`, `reports/stageF_*.json`, and `AQUANET_Q4_PLAN.md`.

Audit status: IEEEtran compilation succeeds; citations and cross-references resolve. Author identities, affiliations, acknowledgements, data-license wording, and journal-specific template changes remain submission metadata.
